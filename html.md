My Personal HTML reference.

### Text Elements

The `<p>` Paragraph, is a simple wrapped text line. I consider it the most basic Element.

Inside `<p>`, `<b>` or `<i>` can be used to annotate indivual words.
